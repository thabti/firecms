# Storage Configuration
# Choose one: "firebase", "json", or "sqlite"
STORAGE_TYPE=firebase

# Data Directory (for json and sqlite storage types)
# This is where the JSON file or SQLite database will be stored
DATA_DIR=./data

# Firebase Configuration (only needed if STORAGE_TYPE=firebase)
# Get these values from your Firebase project settings
# https://console.firebase.google.com/ -> Project Settings -> General
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# Optional: Firebase Measurement ID for analytics
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-XXXXXXXXXX

# API Configuration
# The API version is set to v1 by default in the code
# You can customize this in /src/lib/api-utils.ts if needed
