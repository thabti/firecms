# ==========================================
# Storage Configuration
# ==========================================
# Choose one storage adapter by uncommenting the appropriate section below:
# - postgres: Serverless PostgreSQL via Neon (recommended for production)
# - sqlite: Fast local database (recommended for development)
# - json: Simple file-based storage (good for prototyping)

# --- Option 1: PostgreSQL/Neon (Recommended for Production) ---
# Serverless PostgreSQL with auto-scaling
# Sign up at https://neon.tech for free tier (512MB storage)
# STORAGE_TYPE=postgres
# DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# --- Option 2: SQLite (Recommended for Development) ---
# Fast, zero-config local database
# STORAGE_TYPE=sqlite
# DATA_DIR=./data

# --- Option 3: JSON File Storage (Simplest Setup) ---
# File-based storage, easy to version control
STORAGE_TYPE=json
DATA_DIR=./data
